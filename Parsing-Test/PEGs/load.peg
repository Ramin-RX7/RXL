grammar LOAD
  load  <-   indent:spaces? "load " spaces? pkgs:(pkg_name (spaces? "," spaces? pkg_name )* ","?) spaces?
  spaces   <-   " "*
  pkg_name <-   [a-zA-Z_]+ [a-zA-Z0-9_]*

  
  #list(map(str.strip,x.pkgs.text.split(",")))
